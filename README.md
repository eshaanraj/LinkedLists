# Linked List Project

This project implements a **Linked List** data structure in Java.  
It includes core functionalities such as node insertion, deletion, and traversal, along with a simple command-line interface for testing.

## 📁 Files
- `Node.java` – Defines the node structure for the linked list.  
- `LinkedList.java` – Implements linked list operations (insert, delete, count, etc.).  
- `Listmenu.java` – Provides a menu-based interface to interact with the linked list.  
- `OddMenu.java`, `StackQue.java` – Additional classes for testing and variations.

## ⚙️ How to Run
1. Open the folder in **VS Code** (or any IDE with Java support).  
2. Make sure JDK is installed (Java 17+ recommended).  
3. Compile and run:
   ```bash
   javac *.java
   java Listmenu
